import React from 'react';

import styles from './Card.module.scss';

interface CardProps {
  title: string;
  description: string;
  isShowDescription: boolean;
  isImage: boolean;
}

const Card = () => {
  return <div></div>;
};

export default Card;
